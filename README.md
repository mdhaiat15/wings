```shell
clone project repository
composer install
npm install
php artisan key:generate
php artisan storage:link
create file .env or copy from .env.example
buat database
php artisan migrate
php artisan db:seed --class=SeederUser
php artisan db:seed --class=DatabaseSeeder (if You want)
php artisan serve -> terminal 1
npm run dev -> terminal 2
```

```
Feature
- Login with Username
- Display Product
- Menggunakan PHP Laravel 11, ajax, jquery, tailwindCSS, Boostrap
- Report Penjualan Page
- Cart Page
```
